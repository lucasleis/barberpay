# Base de datos PostgreSQL
DB_NAME=peluqueria_db
DB_USER=admin
DB_PASSWORD=admin123
DB_HOST=localhost
DB_PORT=5432
SECRET_KEY=clave_secreta_muy_larga_y_aleatoria

# Zona horaria
TZ=America/Argentina/Buenos_Aires

# PostgreSQL (Docker Compose)
POSTGRES_DB=peluqueria_db
POSTGRES_USER=admin
POSTGRES_PASSWORD=admin123

# ──────────────────────────────────────────────────────────
# Flask-Mail — Configuración SMTP para envío de recibos
# ──────────────────────────────────────────────────────────
#
# Gmail (recomendado):
#   1. Activar verificación en 2 pasos en tu cuenta Google.
#   2. Ir a https://myaccount.google.com/apppasswords
#   3. Crear una App Password para "Correo / Otro dispositivo".
#   4. Copiar la contraseña de 16 caracteres en MAIL_PASSWORD.
#
# Otro proveedor SMTP (ej. Outlook, SMTP propio):
#   - Cambiar MAIL_SERVER y MAIL_PORT según corresponda.
#   - SSL (puerto 465): MAIL_USE_TLS=false  MAIL_USE_SSL=true
#   - STARTTLS (puerto 587): MAIL_USE_TLS=true  MAIL_USE_SSL=false
#
MAIL_SERVER=smtp.gmail.com
MAIL_PORT=587
MAIL_USE_TLS=true
MAIL_USE_SSL=false
MAIL_USERNAME=tu_cuenta@gmail.com
MAIL_PASSWORD=xxxx_xxxx_xxxx_xxxx
MAIL_SENDER_NAME=BarberPay
